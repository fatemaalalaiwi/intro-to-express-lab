<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> <%= title %></title>
</head>
<body>
    <h1>Greeting Page </h1>
   
    <p> Hello there, <%=userName%>!</p>
       
   <ul>

        <% collectible.forEach((item,index) => { %>
          <li>  <%= item.name  %> : <%= item.price %></li>   
        <% }) %>

   


   </ul>
</body>
</html>